[project]
name = "clearpath-justice-agent"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
    "httpx",
    "pydantic",
    "python-dotenv"
]

[tool.vercel]
entrypoint = "app.main:app"